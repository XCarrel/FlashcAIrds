<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title || 'Flashcards' }}</title>
  <script src="https://cdn.tailwindcss.com"></script>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body class="min-h-screen w-screen font-sans bg-gray-50">
  <div class="container mx-auto p-4">
    <nav class="flex justify-between items-center mb-8">
      <a href="/" class="text-2xl font-bold text-blue-600">Flashcards</a>
      <div>
        @if(auth.user)
          <span class="mr-4">Bonjour {{ auth.user.username }}</span>
          <form action="/logout" method="POST" class="inline">
            {{ csrfField() }}
            <button type="submit" class="text-red-500 hover:text-red-700">Déconnexion</button>
          </form>
        @else
          <a href="/login" class="mr-4 text-gray-600 hover:text-gray-900">Connexion</a>
          <a href="/register" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Inscription</a>
        @end
      </div>
    </nav>
    
    @if(flashMessages.has('error'))
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        {{ flashMessages.get('error') }}
      </div>
    @end
    @if(flashMessages.has('success'))
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
        {{ flashMessages.get('success') }}
      </div>
    @end

    {{{ await $slots.main() }}}
  </div>
</body>
</html>
